# NPM Configuration for Troud Project
# This file ensures consistent dependency resolution across all environments

# Use official npm registry
registry=https://registry.npmjs.org/

# Enable better progress reporting
progress=true

# Increase timeout for slower networks
timeout=60000

# Enable strict peer deps (no legacy support needed)
strict-peer-deps=false 